com:
  example:
    demo-url: http://localhost:${wiremock.server.port}

spring:
  sleuth:
    sampler:
      probability: 1.0
    baggage:
      remote-fields: user-id
      correlation-fields: user-id
  zipkin:
    enabled: true

